/* app.wxss —— 全局样式（详见 04-wxss） */
page { background: #f4f4f4; color: #222; font-size: 30rpx; }
.card { background: #fff; margin: 20rpx; padding: 24rpx; border-radius: 16rpx; }
.title { font-size: 34rpx; font-weight: bold; margin-bottom: 16rpx; }
.btn { margin: 16rpx 0; }
